# CTDD dataset configuration for CF-YOLO.
#
# Update `path` to the local CTDD root after receiving or preparing the dataset.
# The expected structure is documented in DATASET.md.

path: data/CTDD
train: images/train
val: images/val
test: images/test

names:
  0: defect
